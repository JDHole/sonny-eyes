# Minimal set actually imported by eyes/, scripts/ and tests/ (grep for
# import/from ... import, then mapped to pip names: cv2 -> opencv-contrib-
# python, PIL -> pillow, skimage -> scikit-image, colour -> colour-science).
# Versions pinned from requirements.lock. This is enough to run measure/
# batch/review/profile/dupes/status/stop/mcp - no torch, scenedetect,
# imagededup, rawpy or siti-tools, which the shipped code never imports.
# For the author's full development environment see requirements.lock.
click==8.5.0
colour-science==0.4.7
matplotlib==3.11.1
mcp==2.1.1
numpy==2.5.2
opencv-contrib-python==5.0.0.93
pillow==12.3.0
psutil==7.2.2
scikit-image==0.26.0
